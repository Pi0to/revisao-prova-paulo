#include <stdio.h>

int main() {
    
    int n, i=1, b=1;
    
    printf("Digite um numero inteiro positivo:");
    scanf("%d", &n);
    
    while(i<=n){
        b *= i;
        i++;
    }
    
    printf("O fatorial de %d eh %d", n, b);
    
    return 0;
}
