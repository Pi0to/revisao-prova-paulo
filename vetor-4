#include <stdio.h>
#define t 10

int main() {
    
    int v[t], i;
    
    
    for(i=0;i<t;i++){
        printf("Digite o %d° valor: ", i+1);
        scanf("%d", &v[i]);
    }
    
    for(i=0;i<t;i++){
        
        if((v[i] % 2) == 0) printf("%d\t", v[i]);
    }
    
    return 0;
}
