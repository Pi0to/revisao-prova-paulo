#include <stdio.h>
#define t 5

int main() {
    
    int v[t], i, s=0;
    
    
    for(i=0;i<t;i++){
        printf("Digite o valor da celular %d do vetor: ", i);
        scanf("%d", &v[i]);
        
        s += v[i];
    }
    
    printf("a soma dos numeros eh %d", s);
    
    return 0;
}
