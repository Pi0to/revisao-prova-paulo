#include <stdio.h>
#define t 5

int main() {
    
    int v[t], i;
    
    
    for(i=0;i<t;i++){
        printf("Digite o valor da celular %d do vetor: ", i);
        scanf("%d", &v[i]);
    }
    
    for(i=0;i<t;i++){
        printf("%d\t", v[i]);
    }
    
    return 0;
}
